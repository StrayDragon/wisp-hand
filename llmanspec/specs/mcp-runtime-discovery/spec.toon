kind: llman.sdd.spec
name: "mcp-runtime-discovery"
purpose: "定义连接前与连接后的 runtime discovery/preflight 契约，使外部接入方能够在连接前发现阻塞项，并在连接后获取 live runtime 元数据用于诊断与关联。"
valid_scope[1]: "llmanspec/specs/mcp-runtime-discovery"
requirements[3]{req_id,title,statement}:
  r1,外部接入方必须可以在连接前执行 runtime preflight,"Wisp Hand MUST 提供 `wisp-hand doctor --json` 作为连接前预检入口，并返回机器可读的 runtime discovery 报告。该报告至少必须包含 `version`、`config_path`、`transport`、依赖检查结果、关键路径可写性、启用能力摘要、总体 `status` 与阻塞问题列表。"
  r2,已连接客户端必须可以发现 live runtime 元数据,"`wisp_hand.capabilities` MUST 在现有能力矩阵基础上，返回当前运行实例的元数据，至少包含 `version`、`runtime_instance_id`、`started_at`、`transport`、`implemented_tools`、能力可用性、依赖检查摘要与 retention 摘要。"
  r3,discovery 输出必须显式描述 transport 契约,"连接前和连接后的 discovery 输出 MUST 以稳定字段显式描述当前 transport；当 transport 为网络模式时，还必须暴露外部接入所需的绑定信息。"
scenarios[5]{req_id,id,given,when,then,feature}:
  r1,preflight-ready,"操作方在有效配置、依赖完整且关键写路径可写的环境下执行 `wisp-hand doctor --json`","命令 MUST 返回结构化 JSON 报告","把总体 `status` 标记为 `ready`",false
  r1,preflight-blocked,"操作方执行 `wisp-hand doctor --json` 时存在配置非法、关键依赖缺失或关键路径不可写","命令 MUST 以非零状态退出","返回包含阻塞项详情的结构化报告",false
  r2,connected-metadata,"客户端连接到一个已启动的 Wisp Hand 实例并调用 `wisp_hand.capabilities`","服务 MUST 返回当前实例的 `runtime_instance_id`、`started_at`、`transport` 与完整 discovery 摘要","",false
  r2,restart-new-instance-id,"客户端在服务重启前后分别调用 `wisp_hand.capabilities`","两次结果中的 `runtime_instance_id` MUST 不同","客户端可识别 runtime 已切换到新实例",false
  r3,transport-description,"runtime 配置使用不同 transport","discovery 输出 MUST 明确标记 transport 类型并在网络模式下返回绑定信息","",false
