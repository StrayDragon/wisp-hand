kind: llman.sdd.spec
name: "session-runtime"
purpose: "定义 session 生命周期、scope envelope 与基础能力自检契约，作为所有后续副作用与观察工具的公共 runtime 基座。"
valid_scope[1]: "llmanspec/specs/session-runtime"
requirements[4]{req_id,title,statement}:
  r1,基础能力必须可自检,"Wisp Hand 服务 MUST 暴露 `wisp_hand.capabilities` 工具，用于报告当前环境的能力状态、关键依赖以及缺失项，而不是把环境探测分散到后续功能里。"
  r2,服务必须支持显式 session 生命周期,"Wisp Hand 服务 MUST 通过 `wisp_hand.session.open` 与 `wisp_hand.session.close` 管理显式 session，并为每个 session 维护唯一 `session_id`、默认 scope、`armed`、`dry_run` 与 `expires_at` 状态。"
  r3,Session 必须绑定统一的 scope envelope,"每个 session MUST 绑定一个标准化的 scope envelope，至少包含 `type`、`target`、坐标空间元数据与运行约束，以便后续 observe、input、batch、vision 工具共享同一作用域上下文。"
scenarios[6]{req_id,id,given,when,then,feature}:
  r1,capability-matrix,"客户端调用 `wisp_hand.capabilities` 且运行环境满足基础依赖","服务返回 `hyprland_detected`、`capture_available`、`input_available`、`vision_available`、`required_binaries`、`missing_binaries` 等结构化字段","",false
  r1,missing-deps-downgrade,"客户端调用 `wisp_hand.capabilities` 且 `hyprctl` 或其他关键依赖缺失","服务 MUST 返回缺失项列表与能力降级结果","不崩溃、挂起或输出非结构化报错",false
  r2,open-session,"客户端调用 `wisp_hand.session.open` 并提供 scope、arming、dry-run 与 TTL 参数","服务返回新的 `session_id`、标准化后的 `scope`、`armed`、`dry_run` 与 `expires_at`","",false
  r2,closed-session-rejected,"客户端关闭某个 session 后继续使用该 `session_id`","服务 MUST 返回结构化的 `session_not_found` 错误","",false
  r2,expired-session-rejected,"某个 session 的 TTL 到期后又被后续工具引用","服务 MUST 返回结构化的 `session_expired` 错误并阻止继续执行","",false
  r3,standardized-envelope,"后续工具通过 `session_id` 读取 session","它们 MUST 获取同一份标准化 scope envelope","不各自重新解释 scope 参数",false
